spring:
  application:
    name: profile-service
  config:
    import: "optional:configserver:http://localhost:8888"

  cloud:
    config:
      label: main
      profile: default
      fail-fast: true