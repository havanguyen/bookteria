spring:
  application:
    name: identity-service
  config:
    import: "optional:configserver:http://localhost:8888"

  cloud:
    config:
      label: main
      profile: default
      fail-fast: true
